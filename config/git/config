[log]
    date = iso
[alias]
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    pr = pull --rebase origin master
    ps = push
    st = status
    co = checkout
    cb = checkout -b
    br = branch
    cm = commit -s -m
    ca = commit --amend -s --no-edit
    d = diff
    ds = diff --staged
    dc = diff --cached
[user]
    name = Ye Sijun
    email = junnplus@gmail.com
    signingkey = 0582626C83FA9CD0
[core]
    editor = nvim
    excludesfile = ~/.config/git/ignore
[url "https://"]
    insteadOf = git://
[gpg]
    program = gpg
[commit]
    gpgsign = true
; [pager]
;     difftool = true
; [diff]
;     external = difft
;     tool = difftastic
; [difftool]
;     prompt = false
; [difftool "difftastic"]
;     cmd = difft "$LOCAL" "$REMOTE"
